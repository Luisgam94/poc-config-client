spring:
  application:
    name: legacy-datasource
  cloud:
    config:
      uri: http://ms-config-server:8888 #for docker connect in network
  profiles:
    active: prod
#http://localhost:8888 #for local
